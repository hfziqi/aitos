graph hide {
  meta: { description: "Hide context menu", type: "core", output: "removes context menu DOM" }

  let removeMenu = removeElement(id: "contextMenu")
  let removeOverlay = removeElement(id: "contextMenuOverlay")
}
