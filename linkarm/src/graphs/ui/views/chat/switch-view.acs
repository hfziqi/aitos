graph switch-view {
  meta: { description: "Switch main view to chat without rebuilding layout", type: "core", output: "chat view DOM" }

  let clearMainViewContainer = clearElement(id: "mainViewContainer")
  let execChatView = execFile(file: "ui/views/chat/create")
}