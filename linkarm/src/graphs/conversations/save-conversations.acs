graph save-conversations {
  meta: { description: "Save conversations index to local storage", type: "core", input: "conversations", output: "chat/conversations.json" }

  let getConversations = get(key: "conversations")
  let saveIndex = writeLocal(key: "chat/conversations.json", value: "{{getConversations}}")
}